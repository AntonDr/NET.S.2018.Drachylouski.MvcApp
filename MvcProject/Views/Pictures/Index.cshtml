@model IEnumerable<MvcProject.Models.Picture>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Add Picture", "Create","Pictures",new { id = "linkAddImage"},null)|
    @Html.ActionLink("Delete Image","Delete","Pictures", new { id = "linkAddImage" }, null)
</p>

@Html.Partial("~/Views/Shared/PicturePartial.cshtml",Model)

@*<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Content)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
          
            @Html.DisplayFor(modelItem => item.Content)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PictureId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PictureId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PictureId })
        </td>
    </tr>
}

</table>*@
